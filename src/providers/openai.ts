// ============================================================
// AI Gateway — OpenAI Provider Adapter
// ============================================================

// OpenAI is already OpenAI-compatible, so we use the generic adapter
export { adaptRequest, adaptResponse, adaptStreamChunk } from './generic';